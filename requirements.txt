# Create a Python 3.10 or 3.11 environment first. Install the CUDA build of PyTorch
# appropriate for the local NVIDIA driver before installing this file.
torch>=2.4.0
torchvision>=0.19.0
# Validated against the existing local ``yolo11`` Conda environment.
ultralytics==8.4.90
opencv-python>=4.10.0
numpy>=1.26.0
Pillow>=10.0.0
PyYAML>=6.0.2
tqdm>=4.66.0
pytest>=8.0.0
# Imported by the official DINOv3 hub/evaluation modules. Keeping these here
# makes the server bootstrap complete even before hubconf.py is imported.
ftfy>=6.3.1
omegaconf>=2.3.0
regex>=2024.0.0
scikit-learn>=1.5.0
submitit>=1.5.0
termcolor>=2.4.0
torchmetrics>=1.4.0
